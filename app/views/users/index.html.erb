<% provide(:title, 'All Users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <%= render @users %>
  <%# Rails infers the render call to @users. Replaces code from next three lines. %>
  <%# <% @users.each do |user| %> %>
    <%# <%= render user %> %>
  <%# <% end %>  %>
</ul>

<%= will_paginate %>